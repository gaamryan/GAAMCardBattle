Drop MP3s here and point config.js -> music entries at them, e.g.
  menu:   { src: "assets/music/menu.mp3", ... }
  frozen_wastes: { src: "assets/music/frozen.mp3", ... }
If a file is missing or fails to load, the built-in synth theme for that
entry plays instead, so the game always has music.
